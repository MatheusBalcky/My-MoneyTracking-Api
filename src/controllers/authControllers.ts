import { Request, Response } from "express";

export function signInControll(req: Request, res: Response) {
  return res.send(200);
}

export function signUpControll(req: Request, res: Response) {
  return res.send(200);
}
